<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TranslucentAppTheme" parent="AppTheme"></style>

    <style name="StartTheme" parent="TranslucentAppTheme">
        <item name="android:windowBackground">@drawable/bg_monkey</item>
    </style>


    <!-- 定义对话框的样式 -->
    <style name="custom_window_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/main_dialog_bg</item>
    </style>

</resources>
